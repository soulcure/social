import 'package:flutter/material.dart';

class LandscapePrivateChannelAccessPage extends StatelessWidget {
  const LandscapePrivateChannelAccessPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
